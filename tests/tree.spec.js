import { assert } from "chai";

import { MinMaxHeap } from "../index";

describe ("tree", () => {
  it ('should return result of find', () => {
    const mmheap = new MinMaxHeap();

    // assert.equal(mmheap.size, 0);
    // assert.equal(mmheap.hasLeft(0), false);
    // assert.equal(mmheap.hasRight(1), false);
    // assert.equal(mmheap.hasParent(1), false);
  });
});